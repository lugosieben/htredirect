services:
  htredirect:
    build:
      context: ..
      dockerfile: build/Dockerfile
    image: htredirect:local
    ports:
      - "80:80"
      - "8080:8080"
    volumes:
      - ../htredirect.yml:/app/htredirect.yml:ro
    restart: unless-stopped
